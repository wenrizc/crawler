[查看原文](https://www.nowcoder.com/feed/main/detail/5ef9194890ea4ce0b44fe5c6364eab91)

基于提供的面试记录，以下是解析出的所有面试问题：

1.  **请介绍一下你的实习经历。**
2.  **项目里面有没有遇到比较有挑战性的事情？**
3.  **你提到用到的这个乐观锁的话是算什么样的一个环节？**
4.  **为什么不用悲观锁或者互斥的那种锁呢？**
5.  **这里包含两个问题：**
    *   为什么你用了乐观锁前面还要加分布式锁呢？我不加行不行？
    *   我可以直接用数据库行锁代替乐观锁么？
6.  **除了这些还有没有其他挑战，讲一下？**
7.  **你刚才提到一个观察者模式，你这个观察模式是利用的什么去实现的？**
8.  **为什么不用Spring自带的 ApplicationEventPublisher 机制或者注解那种去实现？你们业务和场景上是怎么考虑的？**
9.  **你说模块主动订阅感兴趣的配置，那如果后续如果不是Apollo，去替换成其他的一个方案的话，你这个过程会不会受到影响？**
10. **关于配置中心：** 假如一个类里面定义一个静态变量，引用了配置中心的某个配置。类中有一个普通方法引用了这个静态变量。那在这个类加载的过程中，这个静态变量会不会有可能会发生什么？
11. **这里包含两个问题：**
    *   你在使用 redis 的时候涉及到的场景有哪些？
    *   如果是读多写少的场景，怎么保证数据一致？
12. **你做过金融方面，对数据的一致性要求应该更高一些，如果有场景要求强一致，一般有什么解决方案？**
13. **MQ（消息队列）遇到积压怎么去分析和解决？**
14. **有没有用过 CompletableFuture 这样的工具类？这个工具主要都解决了什么问题？**
15. **这里包含两个问题：**
    *   CompletableFuture 用的线程池是什么？
    *   它会有什么样的问题（例如任务较多、耗时很长这种场景）？
16. **在异步非阻塞、异步回调获取数据的场景下，假如开发了一个接口，用户发起一个请求，怎么实现这种同步等待异步调用的结果？**
17. **那这样的话其实对于主线程来说是不是还是属于阻塞？**
18. **如果主线程可以阻塞但是要有一个超时机制，任务要怎么添加处理？**
19. **做一个数据转化：** 假如现在有一个十进制数是41，转成16进制是多少？
20. **手撕代码（算法题）：** 给定一个数组代表股票每天的价格。
    *   请问只能买卖一次的情况下，最大化利润是多少？
    *   日期不重叠的情况下，可以买卖多次（最大化利润是多少）？