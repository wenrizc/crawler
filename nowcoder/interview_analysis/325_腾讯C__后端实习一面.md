[查看原文](https://www.nowcoder.com/feed/main/detail/f335cfe37f694406825c817d89b432f5)

根据提供的面试记录，解析出的所有面试问题如下：

### 手撕代码与优化部分

**1. 反转链表 (手撕代码)**

**2. 基础优化与八股文：**
*   `new ListNode` 后面需要手动 `delete`，如何解决？
*   智能指针的底层实现是怎样的？
*   移动语义是如何实现的？
*   `std::move` 的作用是什么？
*   什么是万能引用、`std::forward` 以及完美转发？
*   如何将写的 `ListNode` 改成支持多类型（即如何使用模版实现）？

**3. LRU缓存机制 (手撕代码)**
*   提示：使用 `unordered_map` + 双向链表实现。

**4. LRU优化与进阶：**
*   如果 `-1` 是合法的 value，`get()` 接口应该怎么优化？
*   如果 value 是一个很大的数据结构，`get()` 内如何避免拷贝？
*   如果 key 是一个很大的数据结构，但 `unordered_map` 和 `ListNode` 都含有 key，如何优化（减少存储开销）？
*   如何禁用 LRU 类的拷贝构造和拷贝运算符？

### 项目部分

**1. RPC 基本流程是什么？**

**2. Reactor 模型是什么？IO 多路复用是什么？**

**3. IO 和业务逻辑是如何分离的？**

**4. 根据 CPU 核数如何分配 IO 线程和业务线程的数量？**