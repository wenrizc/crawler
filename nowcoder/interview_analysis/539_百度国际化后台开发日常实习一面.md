[查看原文](https://www.nowcoder.com/feed/main/detail/1f49bc06e8f1473085c8c6f3b6fbd424)

根据提供的面试记录，解析出的所有面试问题如下：

### 1. 算法题
*   **LC3. 无重复字符的最长子串**（要求：ACM模式，自己写输入输出）

### 2. 项目问题
*   讲一下项目中缓存穿透、缓存击穿、缓存一致性的解决方案。

### 3. 八股/技术细节问题
1.  **SQL事务场景分析**：
    表 `t` 有主键 `id`，值为 1, 3, 5, 10, 15, 20。事务 A 执行 `SELECT * FROM t WHERE id = 7 FOR UPDATE;`，事务 B 执行 `INSERT INTO t VALUES (6, ...), (8, ...), (11, ...);`。说明这两个事务同时执行时的执行流程，以及结果是失败还是成功？
2.  Mysql的事务是什么？有哪些特性？
3.  Mysql的原子性底层如何实现？（提示：undo log）
4.  讲一下Mysql的MVCC机制的实现原理（提示：readview、undo log、隐藏字段）。
5.  Mysql的覆盖索引、前缀索引、索引下推分别是什么？
6.  如何定位和优化慢SQL语句？（提示：慢查询日志、explain关键字）。
7.  使用explain分析慢SQL语句时，哪些字段比较重要？
8.  Mysql的主从复制的数据同步流程是怎样的？（提示：binlog、offset）。
9.  介绍下Redis的常用数据类型。
10. 讲一下你项目中如何解决缓存穿透、缓存击穿和缓存一致性问题？（注：此为对项目问题的具体发问）
11. 如何使用kafka解决消息丢失问题？
12. 如何使用kafka防止消息的重复消费问题？
13. kafka发送消息时，如果因为网络问题出现消息丢失，如何解决？
14. 从浏览器输入百度的网址访问网页的网络过程是怎样的？
15. HTTP和HTTPS的区别是什么？